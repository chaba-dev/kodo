services:
  web:
    command: mix phx.server
    env:
      DATABASE_PORT: "5432"
      PHX_BIND_IP: "0.0.0.0"
    health: /users/register
    portal:
      title: Kodo
      description: Register a throwaway user to review the development UI.
