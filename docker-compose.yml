services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kodo_dev
    ports:
      - "5435:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d kodo_dev"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - kodo

volumes:
  postgres-data:

networks:
  kodo:
    driver: bridge
